# skald-spec

Provider-native wire contracts shared by prompt authoring, agent runtime, and
workflow execution.

Primary types include `Prompt`, `ProviderRequest`, `ProviderResponse`,
`ResponseType`, provider names, provider-specific request/response structs, and
message conversion helpers.
