[package]
name = "wyrd-tls"
version = { workspace = true }
# Pinned to 2021 to match the source crate (wyrd-bifrost-oracle keeps wyrd-tls on
# edition 2021). The extraction preserves source editions rather than migrating
# code to edition 2024, keeping the port behavior-faithful.
edition = "2021"
license = { workspace = true }
rust-version = { workspace = true }
repository = { workspace = true }
description = "Process-wide TLS crypto-provider ownership for Wyrd transports."

[dependencies]
rustls = { workspace = true }
thiserror = { workspace = true }
