# Operator Card - server-owned notification template.

apiVersion: wyrd/v1
kind: Operator
metadata:
  space: retention
  name: churn-retrain-on-drift
  version: 1.0.0

spec:
  action:
    kind: notify
    channel:
      kind: slack
      text: "Churn drift exceeded its configured threshold."

  budget:
    max_wall_seconds: 600
    max_tool_calls: 20
