{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RegisterOutcome",
  "description": "Whether a register call created a new table or matched an existing one.",
  "oneOf": [
    {
      "description": "A new table was created.",
      "type": "string",
      "enum": [
        "Created"
      ]
    },
    {
      "description": "A table with a matching fingerprint already existed (idempotent).",
      "type": "string",
      "enum": [
        "AlreadyExists"
      ]
    }
  ]
}
